var colors = require(`colors`);
colors.enable();
const { EmbedBuilder } = require("discord.js");

module.exports = {
    name: "guildBanAdd",
    async execute(ban, client) {
        return;
    },
};
